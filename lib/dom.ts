/**
 * Anchor the skip link points at.
 *
 * A constant and not a literal in four files: the link and the `<main>` it
 * targets live in different components, and a typo in either is a skip link
 * that silently does nothing.
 */
export const MAIN_CONTENT_ID = "contenido"
